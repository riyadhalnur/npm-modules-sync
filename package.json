{
  "name": "npm-modules-sync",
  "version": "1.0.1",
  "description": "Keep your global NPM packages in sync between machines.",
  "repository": "riyadhalnur/npm-modules-sync",
  "scripts": {
    "test": "mocha -t 10000 tests/tests.js",
    "lint": "eslint src/**",
    "flow": "flow check",
    "build": "npm run flow && npm run lint && mkdir -p build && flow-remove-types src/ --out-dir build/",
    "coverage": "istanbul cover ./node_modules/mocha/bin/_mocha --report lcovonly -- -R spec tests/tests.js -t 100000 && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage"
  },
  "keywords": [
    "npm",
    "global",
    "modules",
    "sync",
    "machines",
    "computers"
  ],
  "author": "Riyadh Al Nur <riyadhalnur@verticalaxisbd.com> (https://verticalaxisbd.com/)",
  "license": "MIT",
  "engines": {
    "node": ">=4"
  },
  "bin": {
    "npm-sync": "cli.js"
  },
  "preferGlobal": true,
  "devDependencies": {
    "babel-eslint": "^8.2.3",
    "chai": "^4.1.2",
    "chai-as-promised": "^7.1.1",
    "coveralls": "^3.0.1",
    "eslint": "^4.19.1",
    "eslint-plugin-flowtype": "^2.46.3",
    "flow-bin": "^0.72.0",
    "flow-remove-types": "^1.2.3",
    "istanbul": "0.4.5",
    "mocha": "^5.1.1",
    "mocha-lcov-reporter": "^1.3.0",
    "nock": "^9.2.5",
    "sinon": "^5.0.7",
    "sinon-as-promised": "^4.0.3"
  },
  "dependencies": {
    "chalk": "^2.4.1",
    "conf": "^1.4.0",
    "execa": "^0.10.0",
    "gh-got": "^7.0.0",
    "listr": "^0.14.0",
    "meow": "^5.0.0",
    "node-emoji": "^1.8.1",
    "update-notifier": "^2.5.0"
  }
}
